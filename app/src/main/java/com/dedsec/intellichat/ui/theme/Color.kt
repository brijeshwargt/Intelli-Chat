package com.dedsec.intellichat.ui.theme

import androidx.compose.ui.graphics.Color

// New Light Theme Colors
val PrimaryBlue = Color(0xFF007AFF) // A vibrant blue for primary actions
val SecondaryBlue = Color(0xFF5AC8FA) // A lighter blue for secondary elements
val LightGrey = Color(0xFFF2F2F7) // A soft grey for backgrounds
val TextBlack = Color(0xFF1C1C1E) // A dark color for text for high readability
val White = Color(0xFFFFFFFF)

// New Dark Theme Colors (Optional, but good practice)
val PrimaryBlueDark = Color(0xFF0A84FF)
val SecondaryBlueDark = Color(0xFF64D2FF)
val DarkGrey = Color(0xFF1C1C1E) // Background for dark mode
val LightText = Color(0xFFF2F2F7) // Text color for dark mode

val chatBackgroundColor = Color(0xFF5D0E41) // We'll keep this for now, can change later if needed